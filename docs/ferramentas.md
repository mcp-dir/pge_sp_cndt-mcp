# Ferramentas

Procuradoria Geral do Estado SP: Certidão Negativa de Débitos Tributários expõe 1 ferramenta (todas somente leitura).

### 1. `pge_sp_cndt_consultar`
**Input**: `cnpj` (opcional), `cpf` (opcional), `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional)

Procuradoria Geral do Estado SP: Certidão Negativa de Débitos Tributários, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Procuradoria Geral do Estado SP: Certidão Negativa de Débitos Tributários
```
